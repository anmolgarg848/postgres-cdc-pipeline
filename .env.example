# Copy this file to .env and fill in the values. .env is gitignored.
# Values are intentionally left empty — compose fails with a clear message
# if any of them is unset, which is better than shipping a default nobody changes.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
